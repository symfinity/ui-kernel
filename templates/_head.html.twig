{# UI Kernel document head — SSOT for boot script + theme CSS. #}
{# Consumers (showcase apps, ui-profiler shell, …) MUST include; MUST NOT reimplement. #}
{{ ui_kernel_theme_boot_script() }}
{{ ui_kernel_css()|raw }}
